# Add source files here
#EXECUTABLE	:= vectorReduce
EXECUTABLE	:= vectorReduceAtomic
# Cuda source files (compiled with cudacc)
#CUFILES		:= vectorReduce.cu
CUFILES_sm_20		:= vectorReduceAtomic.cu
# C/C++ source files (compiled with gcc / c++)
CCFILES		:= \

################################################################################
# Rules and targets

include ../../common/common.mk
